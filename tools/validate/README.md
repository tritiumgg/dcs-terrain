# tools/validate

The validation sortie: a DCS mission script and the driver that scores a packed
file against the live sim, comparing heights, water, surface types, sightlines
and routes.

It reads no data file from this repository — the packed file under test and the
DCS install are both named on the command line.
