# No terrain data is committed. Extracts and packed files are derived from
# ED terrain data and stay on the user's machine.
*.sqlite
*.sqlite-*
extracts/
*.bin

# Rust
target/
**/*.rs.bk

# Editors and OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Tools built locally rather than downloaded (mise run lua51).
.tools/

# Local agent settings. .claude/settings.json is shared and holds the hooks;
# this one is per-machine and is not.
.claude/settings.local.json
