# One format across the three platforms. CI gates a pull request on
# `cargo fmt --all --check`, so every setting here is a setting nobody argues
# about in review.
#
# The edition is repeated from dcsterrain/Cargo.toml because rustfmt does not
# read it from there: a file formatted without it gets the 2015 rules.
edition = "2024"
max_width = 100
